# Server Configuration
PORT=3000
NODE_ENV=development

# Database
DB_PATH=./data/reports.db

# Admin Configuration
ADMIN_PASSWORD=change_this_password

# Google API Configuration
GOOGLE_API_KEY=your_google_api_key_here

# Gemini Model Configuration (optional)
# Default: models/gemini-2.5-flash
# GEMINI_CHAT_MODEL=models/gemini-2.5-flash
# GEMINI_EMBED_MODEL=models/text-embedding-004

    # Rate Limiting (optional)
    # IMPORTANT: Default is 8 RPM (safe for free tier)
    # Free tier: 10 requests/minute (use 8 to be safe)
    # Paid tier 1: 15 requests/minute (use 12 to be safe)
    # If you get 429 errors, reduce this value or enable billing in Google Cloud Console
    # GEMINI_MAX_RPM=8

# WhatsApp Session (stored in ./data/whatsapp-session)
# No configuration needed - QR code will be shown on first run

